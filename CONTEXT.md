# Image Cropper

以矩形裁切方式批次整理人物圖片的桌面工具，讓使用者逐張確認程式建議的取景範圍並輸出結果。

## Language

**待處理圖片**：
放在 `input` 中、尚待使用者逐張檢視及裁切的靜態圖片或動畫 GIF。
_Avoid_: 原圖、來源圖

**動畫 GIF**：
以 GIF 格式儲存、可含一個或多個影格，且裁切後仍保留播放速度、循環方式及透明背景的待處理圖片。
_Avoid_: GIF 圖片、動圖

**取樣影格**：
從動畫 GIF 選出並進行人物辨識的代表影格；未取樣影格仍會保留在已輸出圖片中。
_Avoid_: 偵測幀、抽樣圖片

**建議裁切框**：
程式辨識人物後，在圖片上自動產生的初始矩形取景範圍；使用者可移動或從四邊、四角調整，但不會重新建立另一個框。
_Avoid_: AI 框、自動框

**人物遮罩**：
程式暫時標記圖片中所有人物範圍的辨識結果，只用來計算建議裁切框，不會成為輸出內容。
_Avoid_: 去背圖、人物裁切圖

**裁切框**：
使用者最後確認的自由比例矩形範圍，框內內容會成為輸出圖片。
_Avoid_: 選取框、切割框

**輸出倍率**：
套用於裁切框內結果的尺寸比例，範圍為 50%～200%；它不改變人物辨識結果或裁切框範圍。
_Avoid_: 縮放倍率、圖片倍率

**輸出尺寸**：
裁切框尺寸套用輸出倍率並四捨五入後，已輸出圖片實際具有的整數像素寬度與高度。
_Avoid_: 預覽尺寸、顯示大小

**預估輸出大小**：
依來源檔案大小及輸出像素數比例即時計算的近似磁碟容量；它不是重新編碼後的實際檔案大小，介面必須標示「約」。
_Avoid_: 輸出檔案大小、預覽大小

**共用裁切框**：
套用於動畫 GIF 所有影格的單一裁切框，確保已輸出圖片的畫布範圍在播放期間保持一致。
_Avoid_: GIF 框、逐格裁切框

**裁切預覽**：
目前圖片套用裁切框後、尚未儲存前的結果預覽。
_Avoid_: 輸出預覽、結果圖

**動畫預覽**：
持續循環播放動畫 GIF 所有影格，並在每個影格上顯示同一個共用裁切框的裁切預覽。
_Avoid_: GIF 預覽、動圖預覽

**已輸出圖片**：
使用者確認裁切框與輸出倍率後，以原始格式與檔名寫入 `output` 的結果；動畫 GIF 仍保有完整影格與播放屬性，成功後流程自動前往下一張待處理圖片。
_Avoid_: 成品、裁切圖

**未處理圖片**：
因格式不受支援、檔案損壞，或讀取、辨識及儲存失敗而無法安全完成裁切的待處理圖片；流程會略過它並在完成畫面列出。
_Avoid_: 錯誤圖片、壞圖
